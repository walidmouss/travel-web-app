<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= name %></title>
  <link rel="stylesheet" href="/css/globals.css">
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    .location-search-bar {
      max-width: 300px; /* Restrict width */
      height: 40px; /* Set a fixed height */
    }
  
    .location-input {
      width: 100%; /* Ensure input spans full width inside the bar */
      height: 100%; /* Align input with the bar height */
    }

    .location-wish-btn {
      color: white; /* Ensure text is always white */
      background-color: var(--primary-color); /* Use custom primary color */
    }

    .location-wish-btn:hover {
      background-color: var(--primary-hover);
      color: white; /* Ensure consistent contrast */
    }
  </style>
</head>

<body class="min-h-screen flex flex-col">
  <!-- Navbar -->
  <nav class="bg-transparent fixed top-0 left-0 w-full py-4 z-10">
    <div class="container mx-auto flex items-center justify-between px-6">
      <a class="text-xl font-bold"><%= name %></a>
      <form method="POST" action="/search" class="location-search-bar flex items-center bg-white rounded-full shadow-lg overflow-hidden">
        <input type="search" name="Search" placeholder="Search" 
          class="p-2 pl-4 text-gray-700 focus:outline-none focus:ring-2 location-input">
        <button type="submit" class="text-white bg-cyan-600 hover:bg-cyan-700 px-4 py-2 rounded-r-full">
          Search
        </button>
      </form>
    </div>
  </nav>

  <!-- Main Content -->
  <div class="container mx-auto mt-20 px-6 grid grid-cols-1 sm:grid-cols-2 gap-8 items-start">
    <!-- Left: Image -->
    <div class="flex justify-center">
      <img src="<%= image %>" alt="Image" class="w-[300px] h-[300px] object-cover rounded-lg shadow-lg" />
    </div>

    <!-- Right: Title and Description -->
    <div>
      <h1 class="text-2xl font-bold text-gray-900 mb-4 text-center sm:text-left">
        <%= name %>
      </h1>
      <p class="text-gray-700 text-justify">
        <%= description %>
      </p>

      <!-- Button Section -->
      <button id="wanttogo" class="location-wish-btn bg-cyan-600 hover:bg-cyan-700 text-white py-2 px-6 rounded-full shadow-lg">
        Add to Want-to-Go List
      </button>
    </div>
  </div>

  <!-- Video Section -->
  <div class="container mx-auto mt-12 px-6">
    <video controls class="rounded-lg shadow-lg w-full">
      <source src="<%= video %>" type="video/mp4">
    </video>
  </div>
</body>

</html>